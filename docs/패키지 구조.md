## 패키지 구조

```shell
application
    └─ dto
    └─ exception
    	└─ FailException
    └─ service
    	└─ concert
    	└─ user
    	└─ userqueue
domain
    └─ entity
    	└─ concert
    	└─ user
    	└─ userqueue
    └─ enums
    └─ repository
    	└─ concert
    	└─ user
    	└─ userqueue
interfaces
    └─ api
        └─ dto
        	└─ request
        	└─ response
        └─ v1
            └─ concert
            └─ user
    └─ exception
        └─ ApiControllerAdvice
```

